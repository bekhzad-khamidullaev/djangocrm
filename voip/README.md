# 📞 VoIP Телефония для Django CRM

Полнофункциональная система VoIP телефонии с веб-интерфейсом управления, аналитикой и интеграциями.

## 🎯 Основные возможности

### 📱 Улучшенная телефонная клавиатура
- **Выпадающее меню** с цифровым блоком (1-9, *, 0, #)
- **Горячие клавиши** для быстрого набора
- **Современный дизайн** с анимациями
- **Интеграция с JsSIP** клиентом

### 🏢 Система SIP настроек (как в Битрикс24)
- **SipServer** - серверы SIP с WebSocket URI
- **InternalNumber** - внутренние номера с автогенерацией
- **SipAccount** - аккаунты пользователей с правами доступа
- **Автоматическое создание** номеров для новых пользователей

### 👥 Группы номеров и маршрутизация
- **NumberGroup** - группы с 5 стратегиями распределения
- **CallRoutingRule** - гибкие правила маршрутизации
- **CallQueue** - очереди звонков с тайм-аутами
- **CallLog** - детальное логирование всех звонков

### 📊 Веб-интерфейсы
- **Дашборд аналитики** с графиками и статистикой
- **Встроенный мониторинг очередей** прямо в Django админке
- **Панель управления** с веб-формами в админке
- **Live виджет мониторинга** с автообновлением каждые 5 секунд

### 🔔 Система уведомлений
- **Email уведомления** о пропущенных звонках
- **Webhook интеграции** для внешних систем
- **Автоматическое создание задач** в CRM
- **Ежедневные отчеты** для менеджеров

### 🔗 Интеграции с PBX
- **Asterisk AMI** - полная интеграция
- **FreeSWITCH ESL** - с автогенерацией dialplan
- **Zadarma webhook** - готовая интеграция

## 🚀 Быстрый старт

### 1. Применение миграций
```bash
python manage.py migrate voip
```

### 2. Настройка первого SIP сервера
```bash
python manage.py setup_sip_system \
    --server-host sip.example.com \
    --websocket-uri wss://sip.example.com:7443 \
    --create-accounts
```

### 3. Доступ к интерфейсам

#### Django Админка
- **Панель управления VoIP**: http://localhost:8000/admin/voip-management/
- **Стандартные модели**: http://localhost:8000/admin/voip/

#### Веб-интерфейсы
- **Дашборд аналитики**: http://localhost:8000/voip/dashboard/
- **JsSIP клиент**: http://localhost:8000/voip/client/

#### Встроенный мониторинг в админке
- **Панель управления VoIP**: http://localhost:8000/admin/voip-management/
  - Встроенный виджет мониторинга с кнопкой "📱 Монитор очередей"
- **Админка очередей**: http://localhost:8000/admin/voip/callqueue/
  - Live мониторинг над списком записей очередей
- **Админка групп**: http://localhost:8000/admin/voip/numbergroup/
  - Мониторинг очередей всех групп

## 🛠️ Команды управления через админку

### Настройка SIP сервера
1. Перейдите в **Админка → VoIP Управление → Настройка SIP сервера**
2. Укажите данные вашего SIP сервера:
   - **Название**: Произвольное имя
   - **Хост**: sip.example.com
   - **WebSocket URI**: wss://sip.example.com:7443
   - **Realm**: (опционально, по умолчанию = хост)
3. Отметьте **"Создать SIP аккаунты"** для автоматического создания

### Создание группы номеров
1. Перейдите в **Админка → VoIP Управление → Создание группы**
2. Заполните форму:
   - **Название**: Отдел продаж
   - **Описание**: Группа менеджеров по продажам
   - **SIP сервер**: Выберите из списка
   - **Стратегия распределения**:
     - `Round Robin` - по очереди
     - `Random` - случайно
     - `Priority` - по приоритету
     - `Ring All` - звонить всем
     - `Least Recent` - кому давно не звонили
   - **Участники**: Выберите внутренние номера
   - **Время звонка**: 30 секунд
   - **Размер очереди**: 10 звонящих

### Создание правила маршрутизации
1. Перейдите в **Админка → VoIP Управление → Правила маршрутизации**
2. Настройте правило:
   - **Название**: Мобильные на продажи
   - **Приоритет**: 100 (меньше = выше)
   - **Паттерн звонящего**: `^\\+79` (российские мобильные)
   - **Действие**: Маршрутизация в группу
   - **Целевая группа**: Отдел продаж

### Просмотр статистики
1. Перейдите в **Админка → VoIP Управление → Статистика**
2. Выберите период и группу для анализа
3. Просмотрите детальную статистику звонков

## 📊 API Endpoints

### Маршрутизация звонков
```bash
# Обработка входящего звонка
POST /voip/incoming/
{
    "caller_id": "+79001234567",
    "called_number": "100",
    "session_id": "call_abc123"
}

# Обновление статуса звонка
POST /voip/status/
{
    "session_id": "call_abc123",
    "status": "answered",
    "answer_time": "2023-12-01T10:30:15Z"
}

# Управление очередями
GET /voip/queue/
POST /voip/queue/

# Статистика звонков
GET /voip/stats/?period=7&group_id=1
```

### Дашборд и мониторинг
```bash
# Статистика дашборда
GET /voip/api/dashboard/stats/

# Производительность групп  
GET /voip/api/dashboard/groups/

# Последние звонки
GET /voip/api/dashboard/recent-calls/

# Живые очереди
GET /voip/api/dashboard/live-queue/

# Статус системы
GET /voip/api/system/status/
```

## ⚙️ Настройка Celery (опционально)

### Установка зависимостей
```bash
pip install celery redis
```

### Настройка в settings.py
```python
# Celery Configuration
CELERY_BROKER_URL = 'redis://localhost:6379/0'
CELERY_RESULT_BACKEND = 'redis://localhost:6379/0'

# VoIP Celery Tasks
from celery.schedules import crontab

CELERY_BEAT_SCHEDULE = {
    'process-missed-calls': {
        'task': 'voip.process_missed_calls',
        'schedule': 60.0,  # Каждую минуту
    },
    'monitor-queue-health': {
        'task': 'voip.monitor_queue_health',
        'schedule': 30.0,  # Каждые 30 секунд
    },
    'generate-daily-report': {
        'task': 'voip.generate_daily_report',
        'schedule': crontab(hour=9, minute=0),  # В 09:00 каждый день
    },
}
```

### Запуск Celery
```bash
# Worker для обработки задач
celery -A webcrm worker -l info

# Beat для периодических задач  
celery -A webcrm beat -l info
```

## 🔗 Интеграция с SIP серверами

### Asterisk AMI
```python
# settings.py
ASTERISK_AMI = {
    'HOST': 'localhost',
    'PORT': 5038,
    'USERNAME': 'django_crm',
    'SECRET': 'your_secret_password',
    'USE_SSL': False,
}

# Запуск интеграции
python manage.py shell
>>> from voip.integrations.asterisk import start_asterisk_integration
>>> import asyncio
>>> asyncio.run(start_asterisk_integration())
```

### FreeSWITCH ESL
```python
# settings.py
FREESWITCH_ESL = {
    'HOST': 'localhost',
    'PORT': 8021,
    'PASSWORD': 'ClueCon',
}

# Запуск интеграции
python manage.py shell
>>> from voip.integrations.freeswitch import start_freeswitch_integration
>>> import asyncio
>>> asyncio.run(start_freeswitch_integration())
```

## 📧 Настройка уведомлений

### Email уведомления
```python
# settings.py
VOIP_EMAIL_NOTIFICATIONS = True
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your-email@gmail.com'
EMAIL_HOST_PASSWORD = 'your-app-password'
DEFAULT_FROM_EMAIL = 'VoIP System <your-email@gmail.com>'
```

### Webhook уведомления
```python
# settings.py
VOIP_WEBHOOK_NOTIFICATIONS = True
VOIP_WEBHOOK_URL = 'https://your-webhook-url.com/voip-events'
```

## 🎨 Интерфейс клавиатуры

### Горячие клавиши
- **0-9, *, #** - Добавление цифр в поле номера
- **Enter** - Совершение звонка
- **Backspace** - Удаление последней цифры  
- **Escape** - Закрытие диалера

### Использование
1. Откройте JsSIP клиент: `/voip/client/`
2. Нажмите на иконку телефона 📞
3. Используйте клавиатуру для набора номера
4. Нажмите "Call" или Enter для звонка

## 🔍 Мониторинг и отладка

### Логи системы
```bash
# Просмотр логов VoIP
tail -f logs/voip.log

# Логи Django
tail -f logs/django.log
```

### Быстрые действия в админке
- **👥 Синхронизировать аккаунты** - создать SIP аккаунты для новых пользователей
- **🧪 Тестировать маршрутизацию** - проверить работу правил
- **🔍 Проверить здоровье системы** - диагностика проблем

### Проверка состояния
```bash
# Команды управления через консоль
python manage.py manage_call_routing stats --group-id 1
python manage.py manage_call_routing list --groups
python manage.py manage_call_routing test --caller-id "+79001234567" --called-number "100"
```

## 🛡️ Безопасность

### Аутентификация webhook'ов
```python
# voip/views/voipwebhook.py
def is_authenticated(request, data):
    # Проверка подписи webhook'а
    # Используется для Zadarma и других провайдеров
```

### Права доступа
- Все административные функции требуют `staff_member_required`
- API endpoints могут быть защищены дополнительной аутентификацией
- SIP пароли генерируются автоматически и хранятся в БД

## 🔧 Troubleshooting

### Частые проблемы

**1. Ошибки импорта при запуске**
```bash
# Проверьте что все миграции применены
python manage.py migrate voip
```

**2. SIP клиент не подключается**
- Проверьте WebSocket URI в настройках сервера
- Убедитесь что сертификат SSL действителен для WSS
- Проверьте доступность порта из браузера

**3. Не работает маршрутизация**
- Проверьте приоритеты правил (меньше = выше)
- Убедитесь что правила активны
- Проверьте regex паттерны

**4. Нет уведомлений о пропущенных звонках**
- Проверьте настройки email в Django
- Убедитесь что Celery запущен
- Проверьте логи на ошибки

### Получение помощи
1. Проверьте логи Django и VoIP модуля
2. Используйте **"Проверить здоровье системы"** в админке
3. Тестируйте маршрутизацию через админку
4. Проверьте статистику системы в дашборде

## 📝 Лицензия

Этот модуль является частью Django CRM системы и распространяется под той же лицензией.

## 🤝 Вклад в развитие

Мы приветствуем вклад в развитие системы VoIP телефонии:

1. Создайте Issue для обсуждения новых функций
2. Отправьте Pull Request с вашими изменениями
3. Добавьте тесты для новой функциональности
4. Обновите документацию при необходимости

---

## 📋 Быстрая справка команд

| Команда | Описание |
|---------|----------|
| `/admin/voip-management/` | Главная панель управления |
| `/voip/dashboard/` | Дашборд аналитики |
| `/voip/monitor/` | Монитор очередей |
| `/voip/client/` | SIP клиент |
| `setup_sip_system --help` | Помощь по настройке |
| `manage_call_routing --help` | Помощь по маршрутизации |

**Система готова к использованию! 🎉**